<p style="color: green"><%= notice %></p>

<%= render @item %>

<div class="container">
  <div class="group text-center" role="group" aria-label="Basic mixed styles example">
    <%= link_to "Edytuj", edit_item_path(@item), class:"btn btn-primary" %>
    <%= link_to "Wróć do strony głównej", items_path, class:"btn btn-success" %>

    <%= button_to "Usuń post", @item, method: :delete, class:"btn btn-danger", style:"margin:10px" %>
  </div>
</div>
-->