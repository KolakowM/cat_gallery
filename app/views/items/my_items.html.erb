# app/views/items/my_items.html.erb

<h1>My Items</h1>

<% if @items.any? %>
  <ul>
    <% @items.each do |item| %>
      <li><%= link_to item.name, item %></li>
    <% end %>
  </ul>
<% else %>
  <p>You have no items yet.</p>
<% end %>
